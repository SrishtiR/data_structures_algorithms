package com.srishti.DSA;

public class DeleteLL extends LinkedListInsertion{
	/* Automatic garbage collection happens in java so we just need to make head equal to null */
	void deleteLL() {
		head = null;
		
	}
}
